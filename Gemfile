source "https://rubygems.org"

# Match GitHub Pages' supported Jekyll and plugin versions during local development.
gem "github-pages", group: :jekyll_plugins

# Ruby 2.6 cannot install ffi 1.17+, so keep the newest compatible native extension.
gem "ffi", "~> 1.16.3"
